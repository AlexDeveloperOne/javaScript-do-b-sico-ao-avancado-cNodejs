const multiplicarPorDois = (x) => {
    return x * 2;
}

console.log(multiplicarPorDois(2));

const multiplicarPorDois2 = x => x * 2;

//arrow function REDUZIDA;
console.log(multiplicarPorDois2(4));
//Se ficar ruim: prezem pela legibilidade do código;

//tenha cuidade ao usar 'arrow funcitions'
//arrow functions não substitui as 'functions'